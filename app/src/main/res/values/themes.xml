<resources>
    <!-- Base application theme. -->
    <style name="Theme.Countries" parent="@style/Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <!-- Secondary brand color. -->

        <!-- Status bar color.
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        -->
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.Countries.Fullscreen" parent="Theme.Countries">
        <item name="android:actionBarStyle">@style/Widget.Theme.Countries.ActionBar.Fullscreen</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

</resources>